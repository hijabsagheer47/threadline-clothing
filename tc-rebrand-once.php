<?php
/**
 * Retired one-off migration. The deploy only adds and updates files, so the
 * original script would otherwise still be sitting on the webspace; this
 * inert stub overwrites it. Safe to delete from the server at any time.
 */
http_response_code(404);
exit;
