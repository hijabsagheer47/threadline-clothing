node_modules/
.DS_Store
*.log

# Local database credentials — never commit config/config.php
config/config.php

# Admin-uploaded images (keep the directories via .gitkeep)
uploads/products/*
uploads/categories/*
!uploads/products/.gitkeep
!uploads/categories/.gitkeep

# Runtime logs
logs/
